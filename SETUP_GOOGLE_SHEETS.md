# 「専門家に相談する」機能をスプレッドシートにつなぐ設定手順

診断結果を見た方が「相談したい」と送信すると、内容をGoogleスプレッドシートに自動で貯められます。
以前AppSheetで使っていたのと同じように、お問い合わせ（リード）を一覧で確認できるようになります。

パソコンでの操作を想定した、①②③の手順で進めてください。難しい設定は不要です。

## ① 受け皿となるスプレッドシートを作る

1. Googleスプレッドシート（sheets.google.com）を開く
2. 「空白」を選んで新しいシートを作る
3. シート名を「iizima診断依頼」などわかりやすい名前に変更する（左上のファイル名部分をクリック）

## ② Apps Scriptを設定する

1. スプレッドシート上部のメニューから「拡張機能」→「Apps Script」を開く
2. 開いた画面に元から入っているコード（`function myFunction() {}` など）をすべて削除する
3. このリポジトリの `google-apps-script/Code.gs` の中身をすべてコピーして貼り付ける
4. 画面上部の保存アイコン（フロッピーディスクのマーク）をクリックして保存する
5. 画面右上の「デプロイ」ボタン →「新しいデプロイ」をクリック
6. 歯車アイコン（種類の選択）から「ウェブアプリ」を選ぶ
7. 「アクセスできるユーザー」を **「全員」** に設定する（これを選ばないとアプリから送信できません）
8. 「デプロイ」ボタンを押す（初回はGoogleアカウントの権限許可を求められるので許可する）
9. 表示された **「ウェブアプリのURL」** をコピーする（`https://script.google.com/macros/s/～/exec` という形のURL）

## ③ アプリ側にURLを設定する

コピーしたURLを、このリポジトリの `assets/app.js` の一番上のほうにある
`CONFIG.sheetsWebhookUrl` に貼り付けます。

```js
const CONFIG = {
  sheetsWebhookUrl: 'ここにコピーしたURLを貼り付け'
};
```

自分で編集するのが難しい場合は、コピーしたURLをそのまま送っていただければ、
このURLを反映したうえでアプリに反映します。

## 動作確認

1. アプリで診断を実行し、結果画面の「📨 専門家に相談する」から名前・連絡先を入力して送信する
2. スプレッドシートの「診断依頼」シートに、送信した内容が1行追加されていれば成功です

## メール通知を追加した場合の更新手順

新しい相談が届いたときにメールでもお知らせするよう `google-apps-script/Code.gs` を更新しました。
すでに①②③の設定を終えている場合は、以下の手順でコードを更新してください（URLは変わりません）。

1. スプレッドシートの「拡張機能」→「Apps Script」を開く
2. 中身をすべて削除し、最新の `google-apps-script/Code.gs` の内容を貼り付けて保存する
3. 画面右上の「デプロイ」→「デプロイを管理」をクリック
4. 一覧に表示されている既存のデプロイの鉛筆マーク（編集）をクリック
5. 「バージョン」を「新しいバージョン」に変更し、「デプロイ」をクリック
6. 初回のみ、Googleアカウントの権限確認（メール送信の許可）が表示されるので許可する

これで、フォーム送信時に `Code.gs` 内の `NOTIFY_EMAIL` に指定したアドレスへ通知メールが届くようになります。
宛先を変更したい場合は、`NOTIFY_EMAIL = '...';` の部分のメールアドレスを書き換えて、同じ手順で再デプロイしてください。
メール通知が不要な場合は `NOTIFY_EMAIL = '';` と空にしてください。

## 補足

- `sheetsWebhookUrl` が空欄のままの場合、「専門家に相談する」欄はアプリ上に表示されません。
- スプレッドシートを更新した場合や、Apps Scriptのコードを修正した場合は、
  「デプロイ」→「デプロイを管理」→ 編集（鉛筆マーク）→「新しいバージョン」を選んで再デプロイしてください。
  （URLは変わりません）
- 個人情報を扱うため、スプレッドシートの共有範囲は必要な社内メンバーのみに限定することをおすすめします。
